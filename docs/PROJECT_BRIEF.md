# Бриф проекта — `procure-ai` (Закупки/PST)

> Источник истины — `docs/ТЗ_Закупки_PST.md` (версия 1.10). Этот документ — навигатор по нему.

---

## Роль

Инженер реализует проект автоматизации обработки предложений поставщиков для ООО «Строительный Берег» (`postroyka.by`). Стек — Bitrix24 + Node.js + Claude Code (headless) + DeepSeek. Работаем строго по ТЗ. Инкрементальные коммиты, прод не трогаем без явного подтверждения.

---

## Архитектура — жёсткие границы

Четыре слоя. **Главный принцип: AI-ядро и парсинг ничего не знают о Б24. Любое обращение к учётной системе — ТОЛЬКО через MCP-сервер.** При переходе на 1С УТ меняется только MCP-слой.

| Слой | Контейнер | Содержимое |
|---|---|---|
| UI | app | `bitrix24/templates-dashboard` поверх Bitrix24 UI Kit |
| Backend | app | Node.js REST API, оркестрация `claude code`, журнал заданий, cron-cleanup |
| AI | app | `claude code` headless + DeepSeek, промпт из `prompts/main.md` |
| MCP | mcp | Собственный MCP-сервер на базе `bitrix24/templates-mcp` |

### Docker — два контейнера

Архитектура разделена на **два образа**:

- **`app`** — backend (Node.js) + UI (статика) + Claude Code CLI + промпты. Оркестрирует обработку файлов, вызывает MCP через stdio/SSE.
- **`mcp`** — MCP-сервер (`bitrix24/templates-mcp`). Единственное место, которое знает о Б24 и обращается к `b24-controller`. При смене учётной системы — меняется только этот контейнер.

Деление обусловлено архитектурным принципом изоляции: `app` не имеет прямого доступа к Б24 API — только через `mcp`.

**Примечание по текущей реализации MCP-сервера.** Неделя 1: инструменты являются заглушками (stub). Начиная с Недели 2 инструменты обращаются к Bitrix24 напрямую через REST-вебхук (`NUXT_BITRIX24_WEBHOOK_URL`). Кастомный `b24-controller`, упомянутый выше, фигурирует в контексте возможного будущего прокси для договоров (`B24_CONTRACTS_API_URL`) — архитектурное решение будет принято в начале Week 2.

---

## Ключевые бизнес-правила (не менять без согласования)

- **Стадия сделки: `C1:NEW`** (воронка id=1 «Закупки»), через ENV `B24_DEAL_DEFAULT_STAGE_ID`.
- **Дубли сделок не проверяем** — сделка создаётся всегда, даже при повторном импорте того же файла. Инструмента `find_existing_deal` нет.
- **Товарные позиции — спецрежим:**
  - Из документа берём цену **БЕЗ НДС** за 1 единицу и количество.
  - Единицу измерения игнорируем — всегда штуки.
  - В Б24: налог **20%**, флаг **«НДС включён в цену» = Y**, цена за 1 из документа, количество.
  - Это намеренно, не баг — не менять.
- **Валюта: только BYN.** Не-BYN → сделка не создаётся, причина в отчёте. Backend проверяет только формат/размер.
- **Допустимые форматы: PDF, XLSX, DOCX.** Legacy XLS не поддерживаем.
- **Матчинг товара:** только активный, только родительский. Несколько найдено → активный с минимальным ID. Два артикула поставщика на один товар (R-12) → один хранится; второй не находим → предупреждение, позиция не привязывается, ручная обработка.
- **Поиск поставщика** — по УНП. Российских (ИНН+КПП без УНП) не ищем.
- **Поиск договора** — через контроллер; точные критерии уточняются при реализации.
- **Отчёт пофайловый.** `GET /job/:id/status` отдаёт статусы по файлам по мере готовности.
- **`create_deal`** прикрепляет исходный файл к карточке сделки и пишет лог обработки в комментарий.
- **Секреты — только в `.env.prod` на сервере.** Не коммитить, не запекать в образ.
- **Реальные инвойсы не коммитить** — держать как фикстуры вне репозитория либо обезличенные.

---

## Структура репозитория

```
procure-ai/
├── docker-compose.prod.yml         # сервисы: app + mcp
├── docker-compose.nginxproxy.yml   # инфраструктура: nginx-proxy + acme (ставится впервые)
├── Dockerfile.app                  # образ: backend + UI(статика) + claude code CLI + prompts/
├── Dockerfile.mcp                  # образ: MCP-сервер (templates-mcp)
├── .dockerignore
├── Makefile
├── .env.prod.example               # см. §12 ТЗ
├── prompts/
│   └── main.md                     # промпт извлечения (входит в app-образ)
├── backend/                        # Node.js: /upload, оркестрация, журнал заданий, cron cleanup
├── mcp/                            # MCP-сервер: find_supplier / find_contract / find_product / create_deal
│                                   # (b24-controller — ВНЕШНИЙ сервис заказчика, в этом репо его нет;
│                                   #  адрес задаётся через B24_CONTRACTS_API_URL)
├── ui/                             # templates-dashboard + UI Kit: загрузка + пофайловый отчёт
├── docs/
│   ├── PROJECT_BRIEF.md            # этот файл
│   └── ТЗ_Закупки_PST.md
└── .github/workflows/
    ├── ci.yml
    └── deploy.yml                  # build → push ghcr.io/<org>/procure-ai; Watchtower подхватывает
```

---

## Переменные окружения (критичные)

| Переменная | Значение / комментарий |
|---|---|
| `VIRTUAL_HOST` / `LETSENCRYPT_HOST` | `purchase.postroyka.by` |
| `NUXT_BITRIX24_WEBHOOK_URL` | вебхук Б24 (имя в коде/`.env.prod`; ранее в брифе — `B24_WEBHOOK_URL`) |
| `B24_CONTRACTS_API_URL` | внешний REST-контроллер договоров (сервис заказчика, не в этом репо) |
| `B24_DEAL_CATEGORY_ID` | `1` |
| `B24_DEAL_DEFAULT_STAGE_ID` | `C1:NEW` |
| `DEEPSEEK_*` | из ТЗ; **в текущей реализации не используется** — агент работает на Claude Code |
| `PUBLIC_PAGE_ENABLED` | `true` (демо) |
| `PUBLIC_PAGE_BASIC_AUTH_USER` | `someDeal` |
| `PUBLIC_PAGE_BASIC_AUTH_PASS` | только на сервере |
| `PUBLIC_PAGE_RESPONSIBLE_USER_ID` | `20` |

---

## План по неделям

| Неделя | Задача Б24 | Содержание |
|---|---|---|
| 1 | #719 | Репо, структура, Dockerfile.app + Dockerfile.mcp, CI/CD-скелеты, backend `/upload`, MCP stub'ы, UI-заглушка, промпт v0 |
| 2 | #721 | REST-контроллер; MCP инструменты (find_supplier / find_contract / find_product / create_deal); промпт v1; сквозной прогон 5+ файлов |
| 3 | #723 | Финальный UI, плейсмент Б24, публичная страница, поллинг, логирование, таймауты, журнал заданий, обработка валюты, cron-cleanup, 15–20 файлов |
| 4 | #725 | Compose-файлы, `.env.prod.example`, Makefile, прод с сисадмином, PAT для GHCR, финальный прогон + демо, передача документации |

---

## Правила работы

- Читать ТЗ целиком перед первым шагом.
- Маленькие осмысленные коммиты.
- **Спрашивать подтверждение** перед: операциями на прод-сервере, удалением данных, изменением прав доступа, коммитом секретов или реальных данных клиента.
- Не коммитить `.env.prod`, ключи, PAT, реальные инвойсы.
- Тест-файлы (реальные инвойсы) — в Google Drive, локально в gitignore-папке фикстур.
- Если бизнес-правило кажется «неправильным» — не менять, спросить.
- Документировать по ходу: README, как править промпт, как откатываться, как читать журнал заданий.
